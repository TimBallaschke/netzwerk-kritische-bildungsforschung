title: Seminarpläne

# Standalone nav section — NOT part of the Aktuelles feed. A curated list
# of shared seminar syllabi, each with a (non-downloadable) PDF.

options:
  template: seminarplaene
  changeTemplate: false
  delete: false

status:
  draft: true
  listed: true

fields:
  # ── Einleitung ──
  introHeadline:
    label: Überschrift
    type: text
    required: true
    placeholder: Seminarpläne teilen und kritische Bildungsforschung pluralisieren
  intro:
    label: Text
    extends: fields/italictext
    size: medium
  # ── Kontakt ──
  lineKontakt:
    type: line
  kontaktHeadline:
    type: headline
    label: Kontakt für Einreichungen
  kontaktText:
    label: Text
    extends: fields/italictext
    size: small
    placeholder: "Wer seinen Seminarplan hier veröffentlichen will, wendet sich an Laura Hanemann:"
  kontaktEmailTitle:
    label: Titel zur E-Mail
    type: text
    default: "Wer seine Seminarpläne veröffentlichen will, wendet sich an:"
    placeholder: "Wer seine Seminarpläne veröffentlichen will, wendet sich an:"
  kontaktEmail:
    label: E-Mail
    type: email
  # ── Seminarpläne ──
  linePlaene:
    type: line
  plaeneHeadline:
    type: headline
    label: Seminarpläne
  plaene:
    label: Einträge
    type: structure
    fields:
      dozierende:
        label: Dozierende
        type: text
        required: true
      titel:
        label: Titel des Seminars
        type: text
        required: true
      pdf:
        label: PDF (Seminarplan)
        type: files
        max: 1
        min: 1
        accept: application/pdf
        required: true
