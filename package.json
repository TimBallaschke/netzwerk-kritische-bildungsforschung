{
  "name": "netzwerk-kritische-bildungsforschung",
  "private": true,
  "scripts": {
    "build:css": "sass --no-error-css --style=expanded assets/style/style.scss assets/style/style.css",
    "test": "node --test tests/*.test.cjs",
    "test:content": "node --test tests/content-coverage.check.cjs"
  },
  "devDependencies": {
    "sass": "1.104.0"
  }
}
